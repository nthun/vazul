% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scramble_values_rowwise.R
\name{scramble_values_rowwise}
\alias{scramble_values_rowwise}
\title{Scramble values rowwise in a data frame}
\usage{
scramble_values_rowwise(data, cols)
}
\arguments{
\item{data}{a data frame}

\item{cols}{\if{html}{\out{<tidy-select>}} Columns to scramble rowwise. Supports helpers like \code{starts_with()}, \code{contains()}, \code{where()}, etc.}
}
\value{
A data frame with values scrambled rowwise across the selected columns.
}
\description{
For each row, shuffle the values across the selected columns.
Columns can be mixed types, but note that R may coerce types during the process (function throws warning if mixed types detected).
}
\examples{
df <- data.frame(
  day_1 = c(1, 4, 7),
  day_2 = c(2, 5, 8),
  day_3 = c(3, 6, 9),
  id = 1:3
)

set.seed(123)
df |> scramble_values_rowwise(starts_with("day_"))
df |> scramble_values_rowwise(c("day_1", "day_2"))

}
\keyword{functions}
